# SPEC

User's own words, verbatim, from the session. Not paraphrased or inferred — if something's missing, ask before adding it here.

## 2026-09-14

> set up a new folder in /Dev/ for a wordpress plugin that does framer like absolute positioning of things in site editor. Check the other plugins for format. Setup the git and install on test site. Just like the others.

(i.e. scaffold the plugin like szm-hover-animations / szm-admin-menu-manager: main .php + assets/editor.js + style.css + editor.css + inc/plugin-update-checker, SPEC/README/TODO/DECISIONS, git init, install on the fse-test site, self-update via GitHub.)